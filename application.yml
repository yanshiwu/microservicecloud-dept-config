spring:
  profiles:
    active:
      -dev
---
spring:
  profiles:
    dev
  application:
    name: microservicecloud-dept-dev

---
spring:
  profiles:
    test
  application:
    name: microservicecloud-dept-test